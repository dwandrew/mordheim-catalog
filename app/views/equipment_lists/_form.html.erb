<%= form_with(model: equipment_list) do |form| %>
  <% if equipment_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(equipment_list.errors.count, "error") %> prohibited this equipment_list from being saved:</h2>

      <ul>
        <% equipment_list.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :warband %>
    <%= form.text_field :warband %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
