<%= form_with(model: mutation) do |form| %>
  <% if mutation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(mutation.errors.count, "error") %> prohibited this mutation from being saved:</h2>

      <ul>
        <% mutation.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :cost %>
    <%= form.text_field :cost %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
