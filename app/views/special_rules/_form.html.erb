<%= form_with(model: special_rule) do |form| %>
  <% if special_rule.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(special_rule.errors.count, "error") %> prohibited this special_rule from being saved:</h2>

      <ul>
        <% special_rule.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
