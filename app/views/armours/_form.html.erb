<%= form_with(model: armour) do |form| %>
  <% if armour.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(armour.errors.count, "error") %> prohibited this armour from being saved:</h2>

      <ul>
        <% armour.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :save %>
    <%= form.text_field :save %>
  </div>

  <div class="field">
    <%= form.label :cost %>
    <%= form.text_field :cost %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
